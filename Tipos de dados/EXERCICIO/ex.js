// Declare uma variável contendo uma string
var curso = 'Ciência da Computação';
// Declare uma variável contendo um número dentro de uma string
var ano = '2025';
// Declare uma variável com a sua idade
var idade = 30;
// Declare duas variáveis, uma com seu nome e outra com seu sobrenome e some as mesmas

var nome = 'Johnny';
var sobrenome = 'Matheus';

var somarNome =  nome + sobrenome;

// Coloque a seguinte frase em uma variável: It's time

var frase = "It's time";

// Verifique o tipo da variável que contém o seu nome

var tipoVariavel = console.log(typeof nome);


//template string

nomeCompleto = `Meu nome é ${nome} ${sobrenome}`;
console.log(nomeCompleto)